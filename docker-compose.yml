version: '3.8'

services:
  # MySQL Database
  mysql:
    image: mysql:8.0
    container_name: luyenviet-mysql
    restart: unless-stopped
    command: --default-time-zone='+07:00'
    expose:
      - "3306"
    environment:
      TZ: ${TZ:-Asia/Ho_Chi_Minh}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-luyenviet}
      MYSQL_USER: ${MYSQL_USER:-app_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-password}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
    volumes:
      - ./mysql_data:/var/lib/mysql
      - ./backend/src/main/resources/db:/docker-entrypoint-initdb.d
    networks:
      - luyenviet-network

  # Backend Spring Boot
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: luyenviet-backend
    restart: unless-stopped
    environment:
      - SPRING_PROFILES_ACTIVE=hub
      - FRONTEND_DOMAIN=http://localhost:1234
      - LOG_PATH=/var/log/luyenviet
      - TZ=${TZ:-Asia/Ho_Chi_Minh}
      - APP_TIME_ZONE=${APP_TIME_ZONE:-Asia/Ho_Chi_Minh}
      - JAVA_TOOL_OPTIONS=-Duser.timezone=${APP_TIME_ZONE:-Asia/Ho_Chi_Minh}
    volumes:
      - ./logs:/var/log/luyenviet
    depends_on:
      - mysql
    networks:
      - luyenviet-network

  # Frontend React (VITE_* cố định trong frontend/Dockerfile lúc build)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: luyenviet-frontend
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Asia/Ho_Chi_Minh}
    ports:
      - "1234:80"
    depends_on:
      - backend
    networks:
      - luyenviet-network

volumes:
  mysql_data:
    driver: local

networks:
  luyenviet-network:
    driver: bridge